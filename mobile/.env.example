# STRATA Mobile — environment config
#
# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
# Values are inlined at build time by `react-native-config`.

SUPABASE_URL=https://mawabokdbrmzegjtvzyz.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here

# Points at the local backend in dev; flip to your deployed URL for release builds.
API_BASE_URL=http://localhost:8080
